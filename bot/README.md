# bot

The trading bot's Python package. The full guide (install, the scout, the pilot, the strategies, the Telegram bot,
every command) is the repository README: [../README.md](../README.md).

```bash
make install        # .venv with Python 3.12
.venv/bin/bot --help
```

Operations handbook: [docs/RUNBOOK.md](docs/RUNBOOK.md).
